#!/bin/sh
# Blocks the commit if the Django project fails ruff lint or format checks.
#
# Activate once per clone with:
#   git config core.hooksPath .githooks

set -e

cd "$(git rev-parse --show-toplevel)/server"

uv run ruff check .
uv run ruff format --check .
